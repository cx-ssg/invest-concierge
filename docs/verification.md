# 综合诊断页抽查结论（verification.md）

> 项目：fund_agent ｜ 页面：`pages/stock_diagnosis.py`（重写交付）｜ 抽查样本：**600519 贵州茅台**
> 抽查方式：**手工核对**（§6-F-3 拍板：不写自动化抽查脚本；临时驱动脚本用后即删）
> 抽查日期：2026-08-30 夜 ~ 2026-08-31 凌晨（沙箱环境，`D:\work\AN\python.exe`，numpy 2.4.6 / pandas 3.0.3 / akshare 1.18.64 / streamlit 1.58.0）
> 对应方案：`docs/DIAGNOSIS_PLAN.md` §7 执行阶段 · §8 验收映射

---

## 0. §6-F 四项拍板落实对账

| §6-F 拍板 | 落实 |
|---|---|
| **1. 护城河锚点**：不改引擎，茅台 50-55 分如实记入 | ✅ 引擎零改动（仍 `akshare` 直取+6 维评分）。本页仅展示引擎真实 `total_score`/`level`/分项/理由。按 moat_analysis.py:308 口径（6 维原始分 ÷60 ×100，≥40 入「🟢 较宽护城河」），§5.4 预判茅台总分落 50-55/较宽档；**沙箱内 EM 行业对比数据不可达无法实测**，真实分以有网复核为准（见 §6 复核清单）。UI 渲染行为已用 52 分夹具验证（见 §3.3）。 |
| **2. 估值历史分位**：补 `ak.stock_a_lg_indicator` 近 5 年分位，失败降级隐藏 | ✅ 已补页面级 `calc_valuation_percentile`：优先乐咕 `stock_a_lg_indicator`；**实测 akshare 1.18.64 已移除该函数** → 回退百度股市通 `stock_zh_valuation_baidu`（同语义近 5 年 PE(TTM)/PB 序列），仍失败则降级隐藏并注明口径（含引擎 `pe_percentile` 为「相对合理 PE」而非历史分位）。**真实出数已验**：600519 PE-TTM 分位 10.5%、PB 6.6%（914 样本，截止 2026-08-30）。 |
| **3. 抽查方式**：手工核对 + verification.md | ✅ 本文档即留痕；仓库不含自动化抽查脚本（`_diag_*.py` 临时驱动已删除）。 |
| **4. AI 4 分析师**：本页不做并行流式 | ✅ 页面为「无 key 引导卡 / 有 key 单按钮→ 单 spinner 顺序出 4 分析师+主席卡+评级徽章」；并行流式明确留给 W2②。 |

---

## 1. 前置门（§8 第一项）

- ✅ 5 引擎 import + 页面模块 import 冒烟零报错（执行：`python -c "import pages.stock_diagnosis"` 等价路径，多轮执行）。
- ✅ `pytest tests/ -q` = **36/36 全绿**（改动后复跑，exit 0）。
- ✅ 页面仅做编排/展示，全部数据经 `data/` 层、AI 经 `utils/ai_helper.py`，未新增 SQL（分层约束合规）。

## 2. 整页门（§8 第 7 项）

- ✅ **600519 全 6 tab 零 traceback**：headless Streamlit `AppTest` 多轮验证 ——
  1. 空状态（引擎全降级）渲染：零异常；
  2. **富数据渲染**（seed 真实快照夹具，见 §3.6）：
     `基本面 50/100 · 排雷 🟡中风险 · 护城河 🟢较宽护城河 · 估值 低估 · 营收 1720.54亿 · 净利 823.20亿 · ROE 32.53% · 毛利率 91.18% · PE分位 10.5% · PB分位 6.6%` —— 全部渲染成功，`FINAL_GATE_PASS`。
- ✅ **无 key 全站可用**：本机 `DEEPSEEK_API_KEY` 为空；页面 5 引擎数据照常，AI tab 显示引导卡（提示配置方式），非空 key 环境行为见 §3.6。
- ✅ **路由与可见性**：`web_agent.py:27` 已注册 `"stock_diagnosis"`、`render_page` 调 `module.main()`；`sidebar.py:79`「🩺 综合诊断」已注册 —— 本页重写零路由改动（与方案 §0 一致）。
- ✅ **整应用入口**：`streamlit run app.py`（headless, port 8577）启动成功，HTTP 200；服务日志无 traceback。
- ⚠️ **手点全流程**：沙箱无浏览器无法人工点击；已用 AppTest（同执行内核：真实跑通 widget 点击→拉取→渲染全链）替代，见第 7 节复核清单第 5 条。

## 3. 逐引擎抽查（§8 第 2-6 项，600519）

> 本沙箱出口代理对东方财富（`push2.eastmoney.com`、`datacenter-web`）**间歇性彻底阻断**（`ProxyError: Remote end closed connection without response`），同花顺 THS / 新浪 / 百度股市通可达。因此按「真实数据可达者验数值、不可达者验降级+渲染路径」双轨记录。

### 3.1 基本面（含财报共享的营收/净利）
- **真实锚点（THS 摘要，2026-08-30 实抓，与引擎同源）**：

  | 年度 | 营业总收入 | 净利润 | 净资产收益率 | 销售毛利率 | 销售净利率 | 资产负债率 |
  |---|---|---|---|---|---|---|
  | 2023 | 1505.60亿 | 747.34亿 | 34.19% | 91.96% | 52.49% | 17.98% |
  | 2024 | **1741.44亿** | **862.28亿** | 36.02% | **91.93%** | 52.27% | 19.04% |
  | 2025 | 1720.54亿 | 823.20亿 | 32.53% | 91.18% | 50.53% | 16.42% |

- **公开年报对照（§5① 锚点 91% / ~30% 量级）**：2024 营业总收入 1741.44亿、归母净利 862.28亿、毛利率 ~91.9% —— **与公开年报事实一致**（锚点达成）。ROE 36.02%（THS 净资产收益率口径）与公开加权 ROE ~36.6% 同量级 ✓。
- ⚠️ **引擎解析缺陷（记录，不改引擎）**：本版 akshare 的 THS 摘要返回**带单位字符串**（`"91.93%"`、`"1741.44亿"`）且**按 1998→最新升序**；引擎 `iloc[0]` 取到最早年度、`safe_float_convert("91.93%")` → 0 —— 导致页面这些字段显示 `--`/「数据缺失」的**降级而非错误**。修复属引擎侧（解析 %/亿 + 取最新行），超出 §6-E 页面范围。
- ✅ 页面富数据渲染行为（营收/净利/ROE/毛利率 cards + 评分条）用真实数值夹具验证零 traceback（§2）。

### 3.2 排雷
- 沙箱内 EM 三表/质押/审计接口不可达 → 8 项全部 `data_available=False`「数据缺失」、评级「✅ 安全」（total_checked=0，**非真实判定**）。页面正确展示「数据缺失项」区块而非空白。
- ✅ 渲染路径（高危/中危/低危计数、触发项 expander、缺失项、综合建议）用演示夹具验证零 traceback（§2）。真实触发数与阈值一致性：待有网复核（§6 第 2 条）。

### 3.3 护城河（§6-F-1 核心记录）
- **引擎未改**；页面展示引擎真实 `total_score/level/分项/理由/优势/不足/建议`。
- 沙箱实测：EM 行业对比（`stock_board_industry_cons_em` 等）不可达 → `_get_industry_data` 提前返回、行业均值字段缺失 → 护城河数据在真实运行中降级为「暂无护城河数据」。
- ✅ **档位 UI 校验**（52 分夹具）：`52/100 → 🟢 较宽护城河`（moat_analysis.py:308-321：raw/60×100，≥40 较宽档）—— 与 §5.4 预判「茅台 50-55 → 较宽档（品牌/规模分项高、网络/资源≈0）」**一致**；真实总分以有网复核为准（§6 第 3 条）。

### 3.4 估值
- **历史分位（§6-F-2）真实出数 ✓**：PE-TTM 当前 19.92 → 近 5 年分位 **10.5%**；PB 当前 6.46 → 分位 **6.6%**（百度股市通 914 样本，截止 2026-08-30）。页面「📈 历史分位」区块渲染并通过。
- 现价锚点：新浪实时 **1297.40 元**（sh600519，+0.40%）—— 与 baidu PE 19.92×EPS≈65 自洽。
- ⚠️ 估值引擎（EM 行情 PE/PB 依赖）沙箱不可达 → 页面降级「数据不足，无法估值」，但**历史分位区块独立渲染**（已解耦，见偏离说明 D4）。
- **偏差 ≤5%、延迟注明**：估值引擎真实输出待有网复核（§6 第 4 条）；页面已固定展示「行情 60s 缓存，估值与东财同源、可能有延迟」caption。

### 3.5 财报三表
- 真实锚点（THS）：2024 营收 1741.44亿 / 净利 862.28亿 / 毛利率 91.93% —— 勾稽检查所需**资产/负债/权益**来自 EM 资产负债表（不可达）→ 页面当前显示「勾稽检查需三项齐全（当前数据缺失）」。
- ✅ 勾稽通过/差异两种渲染分支用夹具验证（3000=500+2500 亿 → 通过）零 traceback；趋势表（报告期×营收/净利/毛利率/ROE）用 THS 2022-2025 行夹具渲染 ✓。
- ⚠️ 利润表/资产负债表/现金流量表（EM）沙箱不可达 → latest 营收/净利在真实运行中 `--`；有网复核（§6 第 4 条）。

### 3.6 AI tab
- ✅ **无 key 路径**：AppTest 无 key 环境渲染「AI 功能未配置 + 配置指引」，零 traceback（§2 覆盖）。
- 有 key 路径：`multi_agent_stock_analysis` 逻辑已有 `tests/test_multi_agent.py` 锚定；页面侧按钮→spinner→4 分析师卡+主席卡+评级徽章的代码路径已实现，**本沙箱无 key 无法端到端实测**，列入 §6 复核清单第 6 条（属 W2② 前置）。

## 4. 环境与依赖发现（写回结论，服务后续排障）

1. **akshare 1.18.64 已移除 5 个旧接口**：`stock_a_lg_indicator`、`stock_dividents_cninfo`（新名 `stock_dividend_cninfo`）、`stock_growth_ability_em`、`stock_pledge_summary_em`、`stock_rd_em` 均 `hasattr=False`；另 `stock_yjkb_em` 签名为 `(date=...)` 而非 `(symbol=...)`。影响：股息率/成长能力/质押/研发占用字段 → None（引擎内 try/except 已兜底，页面降级显示）。
2. **THS 摘要返回值形态**（见 §3.1）：升序 + 带单位字符串 —— 是基本面/护城河多项指标在页面上偏空的根因之一（引擎解析层，非页面）。
3. **numpy 2.4.6 + 旧版 pyarrow/matplotlib 运行期崩**：`st.dataframe`/`st.line_chart`/`matplotlib` 在本机直接 `_ARRAY_API not found`。**方案 §6-B-1 预判成真** —— 本页已改为 markdown 表格（见偏离说明 D1），在任意栈零 traceback。
4. **沙箱出口代理对东方财富间歇阻断**：`ProxyError: Remote end closed connection without response`（直连裸路径偶通、业务路径不通）；新浪/THS/百度可达。属执行环境限制，非代码缺陷。

## 5. 偏离说明（impl-notes 记录，均为方案未覆盖的边缘情形，取最保守备选）

| # | 方案原文 | 偏离 | 理由 |
|---|---|---|---|
| D1 | §2.3「表格：优先 st.dataframe」+ 方案 §2.1 趋势图 | 财报指标表/趋势改用 **markdown 表格**（新 `_md_table`），移除本页全部 `st.dataframe`/`st.line_chart` | §6-B-1 所述 numpy2+旧 pyarrow 崩运行期在本机实测成真；表格无第三方依赖，任何栈零 traceback（整页门硬要求优先） |
| D2 | §5.4-A「用 ak.stock_a_lg_indicator」 | 增加**百度股市通 `stock_zh_valuation_baidu` 回退源**（lg 优先，缺失时回退），失败仍降级隐藏 | akshare 1.18.64 实测无 lg 函数；不加回退则本机历史分位列永远隐藏，"失败降级"被常态触发 |
| D3 | 历史分位与估值引擎同区块 | **分位区块独立渲染**（估值引擎 None 时也显示分位） | 分位是独立数据源（baidu 可达、EM 不可达），不应被 EM 行情拖累隐藏 |
| D4 | 页面用 `st.dataframe(hide_index)` 展示 12 项指标 | markdown 管道表 + 拆分为“指标/数值”两列 | 同 D1 |
| D5 | 实时抽查样本 3 只（茅台/宁德/ST） | 本轮仅 **600519**（用户明确拍板"逐引擎接线后立即用 600519 抽查"）；宁德/ST 列入复核清单 | 用户指示优先 |

## 6. 待有网复核清单（在有网机器上执行，配 SDK 后即补全 §8 数值锚点）

> 建议顺带执行 `pip install -U pyarrow numexpr bottleneck`（或降级 `numpy<2`）消除 §4-3 环境地雷；页面本身已不依赖。

1. `python -c "import pages.stock_diagnosis"` + `pytest tests/ -q`（前置门复跑，应 36 绿）。
2. **排雷**：600519 触发项数应 ≤1 且附实数（阈值与引擎一致）；顺带抽查一只 ST 股触发 ≥2。
3. **护城河**：记录 600519 引擎真实 `total_score` 与分项 —— 预期 50-55 → 「🟢 较宽护城河」档；确认品牌分项 ≥7。
4. **估值**：记录 `valuation_status`/`avg_fair_price` 与新浪现价偏差（方案锚点 ≤5%）；确认延迟注明可见；确认 `pe_percentile` 标签为「相对合理 PE」非历史分位（历史分位列见第 2 条已补）。
5. **手点全流程**：`streamlit run app.py` → 侧边栏「🩺 综合诊断」→ 输入 600519 → 6 tab 逐个翻页，肉眼确认无报错、数据日期（报告期）可见。
6. **AI tab（有 key）**：配置 `DEEPSEEK_API_KEY` 后点击「🤖 开始多智能体辩论」，确认 4 分析师卡+主席卡+评级徽章 60s 内出结果（超时与降级符合 error-handling 规范）。
7. **换样本**：300750 宁德时代 / 一只 ST 股，重复整页门（零 traceback + 数值 sanity）。

## 7. 结论

- 页面交付满足 §8 验收可在线验证项：前置门 ✓ / 整页门（600519 全 6 tab 零 traceback，含空数据与富数据双路径）✓ / 无 key 全站可用 ✓ / 历史分位补算（真实出数 PE 10.5% / PB 6.6%）✓ / 路由与侧边栏零改动 ✓。
- §6-F 四项拍板全部按决策落实，护城河口径冲突**如实记录**（50-55 档、不改引擎）。
- 受沙箱网络限制，排雷触发数/护城河真实总分/估值偏差三项数值锚点**未能实测**，已在 §6 给出精确复核步骤；页面在数据缺失时全部按「暂无数据/数据缺失/降级 caption」优雅呈现（error-handling 规范），不产生 traceback。