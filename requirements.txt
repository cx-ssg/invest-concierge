# -*- coding: utf-8 -*-
# 版本区间锁定说明（2026-09-01 实测）：
# - akshare 1.18.x 已移除 stock_a_lg_indicator/stock_dividents_cninfo 等 5 个旧接口（代码已适配降级），
#   上限 <2 防大版本破坏；下限取实测过的 1.18。
# - numpy 2.x 可用（页面已规避 pyarrow 图表），但 <3 防未测试的大版本。
streamlit>=1.28.0
pandas>=1.3.0
numpy>=1.21.0,<3
akshare>=1.18.0,<2
openai>=1.0.0
requests>=2.28.0
matplotlib>=3.5.0
# 根目录 .env 加载（复制 .env.example 为 .env 填 DEEPSEEK_API_KEY）
python-dotenv>=1.0.0
# ai_chat 消息气泡内渲染 Agent 的 Markdown 回复
markdown>=3.4.0
# M0 FastAPI 桥（React 前端 + 桌面壳的 API 层；uvicorn[standard] 带 windows 支持组件）
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
# 路由层请求体校验（fastapi 强依赖，显式声明防单独安装环境缺件）
pydantic>=2.6.0
# M3 桌面壳：pywebview(WebView2 渲染 React) + pystray 托盘 + Pillow 托盘图标
pywebview>=6.0
pystray>=0.19
pillow>=10.0