# ============================================================
# invest-concierge 环境配置模板
#
# 用法：复制本文件为项目根目录的 .env，填入你的 Key 后保存。
#   Windows PowerShell:  Copy-Item .env.example .env
#   Windows CMD:         copy .env.example .env
#   macOS / Linux:       cp .env.example .env
#
# 安全：.env 已被 .gitignore 忽略，真实 Key 不会提交到仓库；
#      本模板（.env.example）不含任何真实密钥，可放心提交。
# 优先级：系统环境变量 > .env 文件（load_dotenv 不覆盖已存在的环境变量）。
# ============================================================

# DeepSeek API Key（可选）。
# 获取：https://platform.deepseek.com/ （注册送免费额度）
# 留空 = 不启用 AI；持仓/行情/财报/估值等其余功能完整可用。
DEEPSEEK_API_KEY=

# DeepSeek 推理模型名（可选，默认 deepseek-reasoner；一般无需改动）
# DEEPSEEK_REASONER_MODEL=deepseek-reasoner